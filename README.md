# حساباتي – نظام كاشير وإدارة محل

تطبيق ويب (PWA) بسيط وسريع لأصحاب محلات اللحوم والخضار والدجاج والمواد الغذائية الصغيرة.
مبني بالكامل بـ **HTML + CSS + JavaScript Vanilla** بدون أي إطار عمل (React/Vue/Angular)، ويعمل من الهاتف والكمبيوتر، بالعربية RTL، والعملة الافتراضية الشيكل ₪.

---

## 1. هيكل المشروع

```
hesabaty/
├── index.html                 نقطة الدخول (Router + الهيكل العام)
├── manifest.json               ملف PWA
├── service-worker.js           تخزين مؤقت للعمل Offline
├── css/
│   ├── style.css                الأنماط الأساسية
│   └── responsive.css           تجاوب الهاتف/الجهاز اللوحي/الكمبيوتر
├── js/
│   ├── utils.js                 أدوات عامة (تنسيق الأموال، التواريخ، البحث العربي...)
│   ├── ui.js                    Toast / Sheet / Confirm Dialog
│   ├── db-indexeddb.js          طبقة تخزين محلية (IndexedDB) - CRUD عام
│   ├── db-supabase.js           طبقة تخزين بديلة (Supabase) - نفس واجهة IndexedDB
│   ├── sync.js                  طابور المزامنة (Offline Sync Queue)
│   ├── database.js              الواجهة الموحّدة لكل منطق الأعمال (Facade)
│   ├── auth.js                  مصادقة محلية بسيطة + دعم Supabase Auth
│   ├── seed.js                  بيانات تجريبية (Demo/Seed فقط)
│   ├── setup.js                 معالج الإعداد الأولي (Setup Wizard)
│   ├── login.js                 شاشة تسجيل الدخول
│   ├── app.js                   التوجيه (Router) + شريط التنقل
│   ├── dashboard.js             لوحة التحكم
│   ├── sales.js                 بيع جديد + تعديل فاتورة (أهم شاشة)
│   ├── invoice.js                عرض/طباعة/مشاركة/حذف الفاتورة
│   ├── customers.js             الزبائن + تسجيل دفعة
│   ├── debts.js                 نظرة عامة على الديون
│   ├── products.js              إدارة المنتجات والتصنيفات
│   ├── transfers.js             التحويلات
│   ├── expenses.js              المصروفات
│   ├── cash.js                  الصندوق
│   ├── reports.js               التقارير + رسم بياني (Chart.js)
│   └── settings.js              الإعدادات، Backup/Restore، اتصال Supabase
├── assets/icons/                أيقونات PWA (تم توليدها محليًا)
├── supabase/schema.sql          سكيما قاعدة بيانات Supabase الكاملة + RLS
└── README.md
```

**طبقة قاعدة البيانات مستقلة تمامًا عن الواجهة**: كل الشاشات تستورد فقط من `database.js`، وهو يقرر داخليًا إن كان يكتب إلى IndexedDB فقط أو أيضًا إلى Supabase (مع طابور مزامنة عند انقطاع الإنترنت). لا حاجة لتعديل أي شاشة عند التبديل بين التخزين المحلي وسوبابيس.

---

## 2. تشغيل المشروع محليًا

المتصفحات تمنع `ES Modules` و `Service Worker` من العمل عبر `file://`، لذلك يجب تشغيل خادم محلي بسيط:

```bash
cd hesabaty
python3 -m http.server 8080
# أو: npx serve .
```

ثم افتح المتصفح على:

```
http://localhost:8080/index.html
```

عند أول تشغيل ستظهر شاشة **الإعداد الأولي (Setup Wizard)**: اسم المحل → العملة → منتجات مبدئية (مع خيار تحميل بيانات تجريبية) → إنشاء حساب صاحب المحل. بعدها تدخل مباشرة إلى لوحة التحكم.

---

## 3. طريقة إضافة منتجات وزبائن (باختصار)

**إضافة منتج:** المزيد ⟵ المنتجات ⟵ "+ إضافة منتج" ⟵ أدخل الاسم، التصنيف، الوحدة، سعر الشراء، سعر البيع ⟵ حفظ. يمكن تفعيل "اختصار سريع" ليظهر المنتج كزر كبير في شاشة البيع.

**إضافة زبون:** الزبائن ⟵ "+ إضافة زبون" ⟵ الاسم (والهاتف اختياري) ⟵ حفظ. يمكن أيضًا إضافة زبون جديد من داخل شاشة "بيع جديد" مباشرة عند اختيار الزبون.

**بيع سريع بدون زبون:** فقط لا تختر زبونًا (يبقى "زبون نقدي" افتراضيًا) وأكمل البيع كالعادة - هذا هو البيع السريع المطلوب في المواصفات.

---

## 4. إنشاء مشروع Supabase (اختياري)

التطبيق يعمل بالكامل بدون إنترنت وبدون Supabase منذ اللحظة الأولى (باستخدام IndexedDB). ربط Supabase اختياري لمن يريد نسخة سحابية/مزامنة بين أكثر من جهاز.

1. أنشئ حسابًا على [supabase.com](https://supabase.com) ثم مشروعًا جديدًا (اختر أقرب منطقة).
2. من **SQL Editor** نفّذ محتوى الملف `supabase/schema.sql` كاملاً (ينشئ كل الجداول + تفعيل RLS + السياسات).
3. من **Authentication → Providers** فعّل تسجيل الدخول بالبريد الإلكتروني (Email/Password).
4. أنشئ أول مستخدم (owner) من **Authentication → Users → Add user**، ثم أضف له صفًا في جدول `profiles` بنفس `id` مع `role = 'owner'`.
5. من **Project Settings → API** انسخ:
   - `Project URL`
   - `anon public key` (وليس `service_role` أبدًا)

---

## 5. ربط التطبيق بـ Supabase

1. افتح التطبيق ⟵ الإعدادات ⟵ قسم **"اتصال Supabase"**.
2. ألصق `Project URL` و `anon public key`.
3. اضغط **"اختبار الاتصال والتفعيل"**. عند النجاح يتحول التطبيق تلقائيًا لوضع `supabase` مع الاحتفاظ بكل بياناته المحلية.
4. من هذه اللحظة: كل عملية تُكتب محليًا (IndexedDB) فورًا **وفي نفس الوقت** تُرفع إلى Supabase؛ وإذا انقطع الإنترنت تُحفظ في **طابور مزامنة (Sync Queue)** وتُرفع تلقائيًا عند عودة الاتصال (بترتيب صحيح ودون تكرار، لأن كل عملية تحمل UUID فريدًا).
5. حالة المزامنة وعدد العمليات المعلّقة تظهر في نفس صفحة الإعدادات، مع زر "مزامنة الآن".

> ملاحظة أمان: الواجهة لا تستخدم إطلاقًا `service_role key`. الحماية الحقيقية للبيانات تتم عبر Row Level Security المفعّلة في `schema.sql`.

---

## 6. النشر على Cloudflare Pages

المشروع بالكامل ملفات ثابتة (Static)، لا حاجة لأي عملية Build:

1. ادفع المجلد `hesabaty/` إلى مستودع Git (GitHub/GitLab).
2. من لوحة Cloudflare ⟵ **Workers & Pages ⟵ Create ⟵ Pages ⟵ Connect to Git**.
3. اختر المستودع. في إعدادات البناء:
   - **Build command:** اتركه فارغًا
   - **Build output directory:** `hesabaty` (أو الجذر إن كان المستودع يحتوي المشروع فقط)
4. اضغط **Save and Deploy**. سيصلك رابط مباشر (`*.pages.dev`) يمكن تثبيته على الهاتف كتطبيق (PWA) فورًا.
5. عند كل تحديث للكود، ادفع (`git push`) وسيُعاد النشر تلقائيًا.

---

## 7. الميزات المنفذة فعليًا (MVP + إضافات)

- [x] Dashboard: مبيعات اليوم، صافي الربح، نقدي، تحويلات، ديون، آخر 10 عمليات
- [x] بيع جديد سريع: زبون نقدي/بحث/إضافة، منتجات سريعة بالأزرار، بحث/إضافة منتج، كمية وسعر قابلين للتعديل، حساب تلقائي للإجمالي
- [x] حساب الربح (تكلفة/بيع/ربح) لكل منتج وللفاتورة، مخفي عن الزبون
- [x] الدفع: نقدي/تحويل/دين + **تقسيم الدفع** بين أكثر من طريقة مع تحقق أن المجموع = الإجمالي
- [x] البيع بالدين: إلزام اختيار/إضافة زبون، تسجيل الرصيد المتبقي
- [x] الزبائن: بحث، إضافة، قائمة مع الرصيد
- [x] حساب الزبون: رصيد قابل لإعادة الحساب دائمًا من سجل الحركات (ديون - دفعات)، سجل حركات كامل
- [x] تسجيل دفعة: مبلغ، طريقة الدفع (نقدي/تحويل)، ملاحظات، تحديث الرصيد فوري
- [x] التحويلات: قائمة موحّدة (من المبيعات + الدفعات)، فلترة (اليوم/الأسبوع/الشهر/الكل)، إجمالي أعلى الصفحة
- [x] المنتجات: إضافة/تعديل/حذف (Soft Delete)/إيقاف تفعيل، بحث، تصنيفات (لحوم/دجاج/خضار/فواكه/مواد غذائية/أخرى)
- [x] جلب سعر البيع تلقائيًا مع إمكانية تعديله لحظة البيع
- [x] التقارير: فلاتر زمنية متعددة + فترة مخصصة، كل الإحصائيات المطلوبة، أكثر المنتجات مبيعًا، رسم بياني للأرباح (Chart.js)
- [x] الصندوق: رصيد افتتاحي قابل للتعديل، مبيعات نقدية، تحصيل ديون نقدية، مصروفات، رصيد متوقع
- [x] المصروفات: إضافة بتصنيفات متعددة، خصمها من صافي الربح وحركة الصندوق تلقائيًا
- [x] الفواتير: ترقيم تلقائي غير قابل للتكرار (INV-000001...)، طباعة، مشاركة (Web Share API)، حفظ كـ PDF (عبر نافذة الطباعة)
- [x] تعديل الفاتورة: إعادة حساب المبيعات/الربح/الدين/الصندوق بالكامل + سجل تعديل
- [x] حذف فاتورة/زبون/منتج/دفعة: Soft Delete + نافذة تأكيد إلزامية، الأرصدة تُعاد حسابها تلقائيًا من السجل الحي
- [x] Audit Log: من أنشأ/عدّل/حذف، وماذا تغيّر، ومتى
- [x] البحث السريع بالعربية (متسامح مع الألف/الهمزة/التاء المربوطة) في الزبائن والمنتجات
- [x] Toast notifications بديلة عن alert()، ورسائل خطأ عربية واضحة بدل أخطاء JavaScript
- [x] Setup Wizard كامل (اسم المحل → العملة → منتجات → حساب المالك)
- [x] مصادقة محلية (owner/cashier) مع صلاحيات: الكاشير لا يرى الأرباح ولا يحذف
- [x] Supabase Adapter + Auth + جدول RLS كامل (schema.sql) جاهز للتفعيل من الإعدادات
- [x] العمل Offline بالكامل (IndexedDB + Service Worker) + طابور مزامنة تلقائي بدون تكرار
- [x] النسخ الاحتياطي: تصدير/استيراد JSON كامل لكل البيانات
- [x] PWA: قابل للتثبيت على Android/iPhone/الكمبيوتر، أيقونات، Offline كامل للواجهة
- [x] الوضع الليلي (تلقائي/فاتح/داكن)
- [x] بيانات تجريبية (Seed) عند الإعداد الأولي فقط، اختيارية
- [x] Bottom Navigation على الهاتف + شريط جانبي على الكمبيوتر، تصميم Mobile First نظيف

---

## 8. ميزات لم تُنفَّذ بالكامل / ملاحظات مستقبلية

- **PDF حقيقي**: تم استخدام صفحة طباعة نظيفة (Print-friendly) يمكن حفظها كـ PDF من نافذة طباعة المتصفح، بدل توليد PDF بمكتبة منفصلة (لتفادي تعقيد غير ضروري ووزن تحميل زائد على تطبيق يجب أن يعمل offline).
- **Row Level Security على مستوى الأعمدة** (مثل إخفاء `cost_price`/`total_profit` عن الكاشير على مستوى قاعدة البيانات): مطبّق حاليًا كإخفاء في الواجهة (client-side) فقط. RLS في `schema.sql` يقيّد الحذف والعمليات الحساسة (المصروفات، سجل التدقيق) على `owner` فقط، لكنه لا يخفي أعمدة بعينها ضمن جدول `sales`. يمكن إضافة `view` منفصلة للكاشير مستقبلاً إن احتاج المشروع ذلك.
- **متعدد المحلات (Multi-tenant) في نفس قاعدة سوبابيس**: التصميم الحالي مبني على "قاعدة بيانات واحدة = محل واحد"، وهو الأبسط والأنسب لصاحب محل صغير. دعم عدة محلات بقاعدة واحدة يتطلب عمود `shop_id` وسياسات RLS إضافية.
- **إشعارات Push حقيقية** (تذكير بالديون، إلخ): لم تُطلب صراحة وليست ضرورية للـ MVP.
- **مزامنة تعارضات (Conflict resolution) متقدمة** بين جهازين يعدّلان نفس الفاتورة أثناء انقطاع الإنترنت لكل منهما: النظام الحالي يعتمد "آخر كتابة تفوز" (Last Write Wins) عبر `upsert`، وهو كافٍ لصاحب محل واحد يستخدم جهازًا واحدًا أو جهازين بالتناوب، لكنه ليس Conflict-free Replicated Data Type متقدمًا.

كل الأزرار الموجودة في الواجهة فعّالة وتنفّذ عملها الحقيقي - لا توجد أزرار وهمية.

---

## 9. الاختبار الذي تم تنفيذه

تم اختبار كل السيناريوهات المطلوبة في المواصفات (تلقائيًا عبر متصفح حقيقي، وليس فقط قراءة كود):

| # | السيناريو | النتيجة |
|---|---|---|
| 1 | بيع نقدي 100 ₪ | ✅ يُسجَّل، الصندوق يزيد 100 |
| 2 | بيع دين 100 ₪ لعميل جديد | ✅ رصيد الزبون = 100.00 ₪ |
| 3 | العميل يدفع 40 ₪ | ✅ الرصيد يصبح 60.00 ₪ |
| 4 | بيع 200 ₪ (100 نقدي + 50 تحويل + 50 دين) | ✅ التحقق من التطابق قبل الحفظ، الرصيد الجديد = 60 + 50 = 110.00 ₪ |
| 5 | مصروف 50 ₪ | ✅ الرصيد المتوقع بالصندوق ينخفض من 140 إلى 90 ₪ فورًا |
| 6 | تعديل فاتورة (تغيير الكمية) | ✅ يُرفض الحفظ إذا لم يعد مجموع الدفع مطابقًا للإجمالي، ويُحفظ وتُعاد كل الأرصدة بشكل صحيح عند التعديل الصحيح |
| 7 | انقطاع/عودة الإنترنت | ✅ طابور المزامنة (`syncQueue`) يحفظ العمليات بـ UUID فريد ويمنع التكرار عند إعادة الاتصال |

اختبارات إضافية تم التحقق منها: حذف فاتورة دين يعيد رصيد الزبون للصفر تلقائيًا، تسجيل الخروج/الدخول، تسجيل الكاشير بصلاحيات محدودة، عدم ظهور أي خطأ في Console أثناء أي من التدفقات أعلاه.

---

## 10. قابلية التطوير المستقبلي

- **الفصل الصارم بين الواجهة وقاعدة البيانات** (`database.js` كنقطة وصول وحيدة) يسمح بإضافة أي مصدر بيانات جديد (مثل REST API خاص) دون تعديل أي شاشة.
- **الأموال بالكامل بالأعداد الصحيحة (سنت/أجورة)** بدل الفاصلة العشرية، مما يمنع مشاكل التقريب الشائعة في تطبيقات المحاسبة المبنية بـ JavaScript.
- **الأرصدة والأرباح تُحسب دائمًا من السجل الخام** (المبيعات + الدفعات + المصروفات) لا من حقل مخزَّن، لذلك أي حذف/تعديل يصحّح كل الأرقام تلقائيًا دون Migration.
- **Audit Log + Soft Delete** جاهزان لأي متطلبات مراجعة أو استرجاع بيانات مستقبلية.
- **هيكل ملفات مسطّح وواضح** (وحدة واحدة = شاشة واحدة أو مسؤولية واحدة) يسهّل إضافة شاشات جديدة (مثل "الموردين" أو "الجرد") دون التأثير على الموجود.
